version: "3"

services:

  tickr_unit_tester:
    build: ../..
    image: aroyd/tickr_unit_tester:${TICKR_UNIT_TEST_IMAGE_TAG:-latest}
    environment:
      ENV: UNIT_TEST 
    # volumes:
    #   - .:/project


